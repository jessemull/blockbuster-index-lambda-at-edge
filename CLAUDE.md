# CLAUDE.md

This repository uses a shared agent governance system.

**Read first:**

1. [`CONTEXT.md`](CONTEXT.md) — mandatory loading order, precedence, non-negotiables, quality gates
2. [`AGENTS.md`](AGENTS.md) — coding rules and project map

Then follow the reading order and docs listed in `CONTEXT.md`.

**Quality gates:**

```bash
make preflight
```

Do not duplicate policy here — `CONTEXT.md` and `docs/` are authoritative.
